
<div class="container mt-5 bg-light p-3">
  <% if @car.photo.attached? %>
    <% @url = cl_image_path(@car.photo.key, height: 300, crop: :scale) %>
  <% else %>
    <% @url = 'https://upload.wikimedia.org/wikipedia/commons/thumb/a/ac/No_image_available.svg/2048px-No_image_available.svg.png' %>
  <% end %>

  <div class="banner" style="background-image: url(<%= @url %>);"></div>

  <div class="row justify-content-around">
    <div class="col-6">
    <div class="bg-light">
      <h1> <%= @car.make %> <%= @car.model %> </h1>
    </div>
    <div class="bg-light">
      <h3> Type: <%= @car.car_type %> </h3>
      <h4> Location: <%= @car.suburb %></h4>
      <p>Transmission: <%= @car.transmission %> </p>
    </div>
    </div>
    <div class="col-4">
    <div class="mb-3 bg-light">
      <h4 class="mb-3">Seats: <%= @car.seats %> </h4>
      <h5 class="mb-3">Price per day: $<%= @car.price %> </h5>
    </div>
    <% if current_user == @car.user %>
      <div class='bg-light'>
        <%= link_to 'Edit', edit_car_path(@car.id), class: 'btn btn-primary mb-2' %>
        <%= link_to 'Delete', car_path(@car.id), class: 'btn btn-danger ml-4 mb-2', method: 'delete', data: { confirm: "Are you sure?" } %>
      </div>
      <% else %>
        <%= link_to 'Book',  new_car_booking_path(@car.id), class: 'btn btn-primary mb-2' %>
      <% end %>
    </div>
  </div>
  <div class="row justify-content-around">
    <div class="col-11">
      <% if current_user == @car.user %>
        <%= link_to 'Back', listings_cars_path, class: 'btn btn-primary mb-2' %>
      <% else %>
        <%= link_to 'Back', cars_path, class: 'btn btn-primary mb-2' %>
      <% end %>
    </div>
  </div>
</div>

 <div class = "container">
    <div>
      <h2>Booking</h2>
      <% if current_user == @car.user %>
        <%@car.bookings.each do |booking| %>
          <div class = "container">
            <h3> <%= booking.user.first_name %> <%= booking.user.last_name %></h3>
            <h4>Start Date</h4><%= booking.start_date %>
            <h4>End Date</h4><%= booking.end_date %>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>






  <div class = "container">
    <div>
      <h2>Reviews</h2>
      <%@car.booking_reviews.each do |review| %>
      <div class = "container">
        <h3> <%= review.booking.user.first_name %> <%= review.booking.user.last_name %></h3>
        <h4>Rating</h4><%= review.rating %>
        <h4>Content</h4><%= review.content %>
      </div>
      <% end %>
    </div>
  </div>
